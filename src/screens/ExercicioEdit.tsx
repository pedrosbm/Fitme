import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ExercicioEdit() {
    return(
        <SafeAreaView>
            <Text>Essa é a tela de edição do exercicio que o usuario adicionou em um determinado treino</Text>
        </SafeAreaView>
    )
}